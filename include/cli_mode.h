#ifndef CLI_MODE_HEADER_FILE
#define CLI_MODE_HEADER_FILE

extern char* customBoardParameters;

void showListOfCommands();
void customBoard(int, char**);
void autoPlayCommand(int, char**, char*);

#endif
